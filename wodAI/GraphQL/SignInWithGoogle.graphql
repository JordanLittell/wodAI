mutation GoogleLogin($idToken: String!) {
  loginWithGoogle(idToken: $idToken) {
    user {
      id
      height {
        value
        unit
      }
      gender
      fitnessLevel
      email
      age
      weight {
        unit
        value
      }
    }
    token
  }
}
