mutation GoogleLogin($idToken: String!) {
  loginWithGoogle(idToken: $idToken) {
    user {
      id
      height
      gender
      fitnessLevel
      email
      age
      weight
    }
    token
  }
}
