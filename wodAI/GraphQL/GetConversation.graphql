query GetConversation($wodId: String!) {
  getConversation(wodId: $wodId) {
    id
    resourceType
    resourceId
    messages {
      id
      role
      content
      createdAt
    }
    createdAt
    updatedAt
  }
}
